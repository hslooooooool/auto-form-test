package vip.qsos.autoform.data_jpa.server

import org.springframework.stereotype.Service
import vip.qsos.autoform.service.IMSessionService

@Service
class IMSessionServiceImpl : IMSessionService {

    override fun save(sessionClient: Any) {

    }

    override fun findById(id: Int): Any {
        return ""
    }

}